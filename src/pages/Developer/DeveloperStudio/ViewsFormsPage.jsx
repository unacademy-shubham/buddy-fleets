import BackendWorkspacePage from '../shared/BackendWorkspacePage';

export default function ViewsFormsPage() {
  return (
    <BackendWorkspacePage
      workspaceKey='leaf:developerstudio:viewsforms'
      title='Views & Forms'
      description='Configure list views, detail views, forms, dashboards and reusable module UI surfaces.'
    />
  );
}
