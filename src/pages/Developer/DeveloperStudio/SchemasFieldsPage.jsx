import BackendWorkspacePage from '../shared/BackendWorkspacePage';

export default function SchemasFieldsPage() {
  return (
    <BackendWorkspacePage
      workspaceKey='leaf:developerstudio:schemasfields'
      title='Schemas & Fields'
      description='Configure fields, validation rules, relationships, calculations and file properties for metadata-driven modules.'
    />
  );
}
