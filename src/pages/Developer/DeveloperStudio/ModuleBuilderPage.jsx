import BackendWorkspacePage from '../shared/BackendWorkspacePage';

export default function ModuleBuilderPage() {
  return (
    <BackendWorkspacePage
      workspaceKey='leaf:developerstudio:modulebuilder'
      title='Module Builder'
      description='Design module identity, fields, views, permissions, company assignment and publishing workflow.'
    />
  );
}
